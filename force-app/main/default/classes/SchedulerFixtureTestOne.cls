@isTest
private class SchedulerFixtureTestOne {
  @isTest
  static void testTrivial() {
    System.assertEquals(
      1,
      1,
      'fixture test class used by Apex Test Scheduler tests'
    );
  }
}
